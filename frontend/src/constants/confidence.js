// Shared confidence styling so pills and dots look identical everywhere.

// Pill badges (background + text), used next to answers.
export const CONFIDENCE_STYLES = {
  high: 'bg-green-100 text-green-700',
  medium: 'bg-amber-100 text-amber-700',
  low: 'bg-red-100 text-red-700',
};

// Solid dots, used in compact lists (e.g. the analytics panel).
export const CONFIDENCE_DOT = {
  high: 'bg-green-500',
  medium: 'bg-amber-500',
  low: 'bg-red-500',
};
